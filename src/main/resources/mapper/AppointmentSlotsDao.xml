<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.AppointmentSlotsDao">

    <resultMap type="com.entity.AppointmentSlotsEntity" id="appointmentSlotsMap">
        <id property="id" column="id"/>
        <result property="packageId" column="package_id"/>
        <result property="appointmentDate" column="appointment_date"/>
        <result property="timeslot" column="timeslot"/>
        <result property="maxReserveCount" column="max_slots"/>
        <!-- 删除 remainingReserveCount 的直接映射 -->
        <result property="addtime" column="addtime"/>
    </resultMap>

    <!-- 查询剩余名额（动态计算） -->
    <select id="getRemainingSlots" resultType="int">
        SELECT (max_slots - reserved_slots) AS remaining_reserve_count
        FROM appointment_slots
        WHERE package_id = #{packageId}
          AND appointment_date = #{appointmentDate}
          AND timeslot = #{timeslot}
    </select>

    <!-- 查询最大名额 -->
    <select id="getMaxSlots" resultType="int">
        SELECT max_slots
        FROM appointment_slots
        WHERE package_id = #{packageId}
          AND appointment_date = #{appointmentDate}
          AND timeslot = #{timeslot}
    </select>

    <!-- 扣减剩余名额（更新 reserved_slots 字段） -->
    <update id="decreaseRemainingSlots">
        UPDATE appointment_slots
        SET reserved_slots = reserved_slots + 1
        WHERE package_id = #{packageId}
          AND appointment_date = #{appointmentDate}
          AND timeslot = #{timeslot}
          AND (max_slots - reserved_slots) > 0
    </update>

    <!-- 恢复剩余名额（更新 reserved_slots 字段） -->
    <update id="increaseRemainingSlots">
        UPDATE appointment_slots
        SET reserved_slots = reserved_slots - 1
        WHERE package_id = #{packageId}
          AND appointment_date = #{appointmentDate}
          AND timeslot = #{timeslot}
          AND reserved_slots > 0
    </update>

</mapper>
